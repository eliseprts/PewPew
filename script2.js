// UTILITY FUNCTION

function getDistance(x1, y1, x2, y2) {
    const xDistance = x2 - x1
    const yDistance = y2 - y1
    return Math.sqrt(Math.pow(xDistance, 2) + Math.pow(yDistance, 2))

}

if (getDistance(object1.x, object1.y, object2.x, object2.y) == 0) {
    // code
}
else {
    // code
}
